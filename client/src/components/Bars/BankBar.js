import React from "react";
import { Link } from "react-router-dom";

const BankBar = ({ items }) => {
  return (
    <div className="bankBar">
      <h1>Hamilton</h1>
    </div>
  );
};

export default BankBar;
