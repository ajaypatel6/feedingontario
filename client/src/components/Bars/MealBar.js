import React from "react";
import { Link } from "react-router-dom";

const MealBar = ({ items }) => {
  return (
    <div className="mealBar">
      <h1>Hamilton</h1>
    </div>
  );
};

export default MealBar;
