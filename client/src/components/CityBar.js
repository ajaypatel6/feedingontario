import React from "react";
import { Link } from "react-router-dom";

const CityBar = ({ items }) => {
  const searchCity = () => {};
  // const {} = items;

  return (
    <div className="cityBar">
      <h1>Toronto</h1>
    </div>
  );
};

export default CityBar;
